PORTAL_BASE_URL=https://urja-ops.flockenergy.tech
PORTAL_USERNAME=operator@urja.local
PORTAL_PASSWORD=urja-ops-2026
CACHE_TTL_SECONDS=300
CACHE_ENABLED=true

# Set to false to disable this API's own JWT auth entirely - every route
# becomes open, and the settings below are ignored. Local dev only.
AUTH_ENABLED=false

# Credentials for THIS API (not the portal) - used by POST /auth/login
API_USERNAME=admin
API_PASSWORD=flock-energy-2026
JWT_SECRET_KEY=12345hhh
JWT_EXPIRE_MINUTES=60
